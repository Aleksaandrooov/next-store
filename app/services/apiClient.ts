import * as productSearch from './product';
import * as filter from './model';
import * as cart from './cart';
import * as favorites from './favorites';
import * as localProducts from './localProduct';

export const Api = {
  productSearch,
  filter,
  cart,
  favorites,
  localProducts,
};
